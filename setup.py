from setuptools import setup

setup(
    name='dragonfly_dome',
    version='0.1',
    packages=['dragonfly_dome'],
    install_requires=[
        # Any specific dependencies that need to be installed along with your package
    ],
)